package com.skripsi.utils

import com.skripsi.data.data_source.DataSource
import org.junit.Test
import kotlin.test.assertEquals

class NaiveBayesUtilTest {/*
    private val items = DataSource.dataTrainingSample()

    @Test
    fun `Test All True barang yang memenuhi target`() {
        val result = items.allTrueCount()

        assertEquals(
            expected = 4,
            actual = result
        )
    }

    @Test
    fun `test all false barang yang tidak memenuhi target`() {
        val result = items.allFalseCount()
        assertEquals(
            expected = 6,
            actual = result
        )
    }*/
/*
    @Test
    fun `test kategori makanan memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.kategoriMakanan(true).size
    )

    @Test
    fun `test kategori makanan tidak memenuhi target`() = assertEquals(
        expected = 1,
        actual = items.kategoriMakanan(false).size
    )

    @Test
    fun `test kategori minuman memenuhi target`() = assertEquals(
        expected = 1,
        actual = items.kategoriMinuman(true).size
    )

    @Test
    fun `test kategori minuman tidak memenuhi target`() = assertEquals(
        expected = 1,
        actual = items.kategoriMinuman(false).size
    )

    @Test
    fun `test kategori kebutuhan memenuhi target`() = assertEquals(
        expected = 1,
        actual = items.kategoriKebutuhan(true).size
    )

    @Test
    fun `test kategori kebutuhan tidak memenuhi target`() = assertEquals(
        expected = 1,
        actual = items.kategoriKebutuhan(false).size
    )

    @Test
    fun `test kategori kosmetik memenuhi target`() = assertEquals(
        expected = 0,
        actual = items.kategoriKosmetik(true).size
    )

    @Test
    fun `test kategori kosmetik tidak memenuhi target`() = assertEquals(
        expected = 3,
        actual = items.kategoriKosmetik(false).size
    )

    //persediaan
    @Test
    fun `test pesediaan banyak memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.persediaanBanyak(true).size
    )

    @Test
    fun `test pesediaan banyak tidak memenuhi target`() = assertEquals(
        expected = 3,
        actual = items.persediaanBanyak(false).size
    )

    @Test
    fun `test pesediaan sedikit memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.persediaanSedikit(true).size
    )

    @Test
    fun `test pesediaan sedikit tidak memenuhi target`() = assertEquals(
        expected = 3,
        actual = items.persediaanSedikit(false).size
    )

    //promosi
    @Test
    fun `test promosi memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.promosiTrue(true).size
    )

    @Test
    fun `test promosi tidak memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.promosiTrue(false).size
    )

    @Test
    fun `test tidak promosi memenuhi target`() = assertEquals(
        expected = 2,
        actual = items.promosiFalse(true).size
    )

    @Test
    fun `test tidak promosi tidak memenuhi target`() = assertEquals(
        expected = 4,
        actual = items.promosiFalse(false).size
    )*/
}